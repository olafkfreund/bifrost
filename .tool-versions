# Pinned external tool versions (#14). The Rust toolchain is pinned separately in
# rust-toolchain.toml; Node is pinned by the devcontainer feature. These are the
# migration CLIs Bifrost wraps — pinned + recorded per job so conversions are
# reproducible and attestable (plan §1, "wrap, don't fork").
#
# Format: one "name version" per line (asdf/.tool-versions style).
gh-actions-importer 1.3.21847
ado2gh 1.7.0
gh 2.93.0
nodejs 22.12.0
