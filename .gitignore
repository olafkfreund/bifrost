# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed — binary workspace, so we want reproducible builds.

# Node / portal
node_modules/
dist/
.vite/

# Jekyll / docs
docs/_site/
docs/.jekyll-cache/
docs/.jekyll-metadata
docs/vendor/
.bundle/

# Env / secrets
.env
.env.local
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local secrets (direnv) — ADO PAT, GitHub token. NEVER commit.
.envrc
