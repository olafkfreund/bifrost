{
  "proposed_yaml": {
    "name": "Publish test results",
    "uses": "dorny/test-reporter@v1",
    "with": { "path": "**/TEST-*.xml" }
  },
  "rationale": "Maps PublishTestResults@2 onto a reporter action that ingests the same JUnit XML.",
  "risk_flags": ["Confirm the test glob matches the migrated build's output path"],
  "verify_steps": ["Compare the parsed test counts against the ADO baseline run"],
  "confidence": 0.7,
  "risk_score": 42,
  "rating": "amber"
}
